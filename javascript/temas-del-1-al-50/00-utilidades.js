// Imprimir titulo del ejercicio
function printTitleWithLine(title) {
    var line = "-".repeat(title.length);
    console.log(line);
    console.log(title);
    console.log(line);
}

export default {
    printTitleWithLine
};
